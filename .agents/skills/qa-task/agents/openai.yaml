interface:
  display_name: "QA NovelHub Task"
  short_description: "Validate acceptance criteria independently"
  default_prompt: "AGENTS.md + .agents/prompts/common.md + project docs; use $qa-task to validate one completed task."
